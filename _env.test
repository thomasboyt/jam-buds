###
# DON'T MODIFY THIS FILE WITH YOUR VARIOUS SECRET THINGS!
#
# Copy-paste it to .env.test and add them there
###

# These are environment variables used only in tests

DATABASE_URL=postgres://your_name_here@localhost:5432/jambuds_test

# Values used for feature testing, so feature test servers can run alongside dev servers
PORT=3001
API_URL=http://localhost:3001

DEV_SERVER_PORT=8080
STATIC_URL=http://localhost:8080

NODE_ENV=test