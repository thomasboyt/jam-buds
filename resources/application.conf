ktor {
    deployment {
        port = 3333
        port = ${?PORT}
    }
    application {
        modules = [ ApplicationKt.mainModule ]
    }
}

jambuds {
    # TODO: should this be hard-coded here?
    database_url = "jdbc:postgresql://localhost:5433/jambuds?user=postgres"
    database_url = ${?JDBC_DATABASE_URL}
}
